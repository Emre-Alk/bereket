<%= render partial:'donations/success', locals: {
  donation: @donation,
  checkout_session: @checkout_session,
  reduction: @reduction,
  after_reduction: @after_reduction,
  favorite: @favorite,
} %>
