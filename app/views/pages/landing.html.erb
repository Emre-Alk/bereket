<%# typewriter %>
<!--
<div class="px-5">
  <div data-controller="typewriter">
    <span data-typewriter-target="text" class="inline-block">landing page here</span>

    <span class="inline-block align-middle text-center bg-green-300 rounded-full p-2"  data-typewriter-target="logo" >
      <%= svg_tag('g', class:"text-black size-6") %>
    </span>
  </div>
</div>
-->

<%# header %>
<div class="space-y-10 ml-5">
  <h1 class="text-xl font-medium roboto-medium-italic tracking-wider">Goodify</h1>
</div>

<%# why Goodify %>
<div class="mt-10 flex flex-col items-center space-y-5">
  <h1 class="text-3xl tracking-wider text-center font-medium">Votre générosité mérite mieux</h1>
  <div class="flex flex-col items-center p-4 rounded-lg gap-y-4">

    <ul class="flex flex-col items-center gap-y-2 text-sm roboto-light ">
      <li class="inline-flex gap-x-4 items-center" >
        <span>
          <%= svg_tag('check', class:"size-4 stroke-2 text-green-800") %>
        </span>
        <p class="tracking-wide">Faites votre don en 4 clics</p>
      </li>

      <li class="inline-flex gap-x-4 items-center" >
        <span>
          <%= svg_tag('check', class:"size-4 stroke-2 text-green-800") %>
        </span>
        <p class="tracking-wide">Faites votre don en 4 clics</p>
      </li>

      <li class="inline-flex gap-x-4 items-center" >
        <span>
          <%= svg_tag('check', class:"size-4 stroke-2 text-green-800") %>
        </span>
        <p class="tracking-wide">Faites votre don en 4 clics</p>
      </li>

    </ul>

    <%= link_to 'Essayer Goodify', new_user_registration_path, class:'w-full py-2 rounded-lg text-center text-base tracking-wide bg-orange-400 text-white shadow-lg active:shadow-inner' %>

  </div>
</div>

<%# sticky call to action %>
<div class="fixed bottom-0 inset-x-0 w-full px-5 py-10 rounded-t-3xl flex flex-col items-center justify-center space-y-5 bg-black">
  <!--
  later to remove link_to and add JS to open pop-up form
  -->

  <% if user_signed_in? %>
    <%= link_to dashboard(current_user), class:'w-full py-2 rounded-lg flex justify-center items-center space-x-4 text-lg tracking-wide bg-white text-black' do %>
      <%= icon('fa-solid', 'circle-user') %>
      <p>Home</p>
    <% end %>

    <%= link_to destroy_user_session_path, data: { "turbo-method": :delete, "turbo-confirm": "are you sure ?"}, class:'w-full py-2 rounded-lg flex justify-center items-center space-x-4 text-lg tracking-wide border-2 border-white bg-black text-white' do %>
      <%= icon('fa-solid', 'arrow-right-from-bracket') %>
      <p>Se déconnecter</p>
    <% end %>

  <% else %>
    <%= link_to new_user_registration_path, class:'w-full py-2 rounded-lg flex justify-center items-center space-x-4 text-lg tracking-wide bg-white text-black' do %>
      <%= icon('fa-solid', 'envelope') %>
      <p>S'enregistrer avec un email</p>
    <% end %>

    <%= link_to new_user_session_path, class:'w-full py-2 rounded-lg flex justify-center items-center space-x-4 text-lg tracking-wide border-2 border-white bg-black text-white' do %>
      <%= icon('fa-solid', 'user') %>
      <p>Se connecter</p>
    <% end %>
  <% end %>

</div>
