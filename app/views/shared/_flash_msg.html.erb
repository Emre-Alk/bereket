<% if !flash.empty? %>
  <%= flash.each do |type, msg| %>
    <div class="fixed top-0 z-50 inset-x-0 h-fit p-5 rounded-lg bg-yellow-100 tracking-wide roboto-medium text-indigo-500"><%= msg %></div>
  <% end %>
<% end %>
